# Roo Code Rules

**Project Name**: api_factory
**Last updated**: 2026-02-03

## Active Technologies
- Dart 3.x / Flutter 3.x + `flutter_riverpod` (State Management), `dio` (Network), `sqflite` (Local DB), `uuid` (ID generation), `path_provider` (File storage). (002-script-to-video-pipeline)
- SQLite for structured data (Scenes, Shots, Tasks), Local File System for media assets (Images, Videos). (002-script-to-video-pipeline)

- Dart 3.x + Flutter 3.x (001-animator-workbench-v2)
- SQLite (001-animator-workbench-v2)

## Project Structure

src/
tests/

## Commands

cd src; pytest; ruff check .

## Language Conventions

Dart 3.x: Follow standard conventions

## Recent Changes
- 002-script-to-video-pipeline: Added Dart 3.x / Flutter 3.x + `flutter_riverpod` (State Management), `dio` (Network), `sqflite` (Local DB), `uuid` (ID generation), `path_provider` (File storage).

- 001-animator-workbench-v2: Added Dart 3.x + Flutter 3.x
